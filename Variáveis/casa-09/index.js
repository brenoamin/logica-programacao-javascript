// Declaração do número de lados de um polígono
const lados=5;
//Cálculo da soma dos ângulos internos
let s = (lados-2)*180;

console.log(`A soma dos ângulos internos é: ${s}°`);

console.log(`Cada ângulo vale: ${s/lados}°`);

