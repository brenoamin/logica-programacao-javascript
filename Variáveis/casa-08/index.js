//Raio do Círculo em metros
const r=0.5;
//Cálculo do Comprimento
const C=2*Math.PI*r
//Cálculo da Área
const A=Math.PI*Math.pow(r,r);

console.log(`O comprimento C desse círculo é: ${C}`)

console.log(`A área A desse círculo é: ${A}`)
