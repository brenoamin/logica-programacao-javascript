const a=3, b=-15, c=12
//Cálculo de delta dado os coficientes a,b e c
let delta = b*b - 4*a*c
if(delta>0){
    console.log(delta = b*b - 4*a*c )
}
else{
    console.log("Não existe soluções reais para o delta inserido")
}